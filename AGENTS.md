# 项目哲学：统一母版，模板优先，而非代码排版

本项目不是“自动生成 PPT 版式”的项目，而是一套持续积累、检索和复用优秀 PPT 母版家族与内容模块的方法。

模型擅长内容策划、文案压缩和图片选择，但不应被信任去从零决定页面布局。稳定的视觉质量来自人类已经验证过的母版外壳和内容模块。一套成品先锁定一个母版家族，封面、目录、章节、正文标题区与结尾保持同一视觉身份；真正按内容变化和积累的是标题以下的内容模块。

这里的“母版家族”不只指 PowerPoint XML 中的 `slideMaster`，而是三者的组合：一个真实 PowerPoint 母版、该母版下获准使用的版式，以及少量固定在幻灯片层的页眉/标题外壳。不能因为页眉对象位于 slide XML 中，就把它误当成可变正文。

项目分为“正式生产”和“模板研发”两条边界清晰的流程。正式生产只使用已审核、已合同化、已验证的资产；`模板研发部门/` 是唯一允许试制新内容模块的隔离区。研发候选必须依附一个既有母版家族，默认一次只设计标题以下的一页内容模块；用户明确要求批量研发时，可以在一份审核稿中制作指定数量、彼此独立的候选页。每页都必须经过明确审核和机械验证后才能进入正式模板库。研发权限不能反向放宽成品制作规则。

## 不可违反的规则

1. **禁止从零生成任何一页成品 PPT。** 正式任务不得用 PptxGenJS、python-pptx、SVG、Canvas、HTML 截图或其他代码自行绘制整页版式。唯一例外是用户明确在 `模板研发部门/` 发起的候选研发：每张候选必须从既有目标母版家族的正文壳副本开始，只能研发标题以下的可编辑内容模块；审核前不得用于任务成品、不得写入活动索引。批量研发只增加候选数量，不放宽单页边界。
2. **一套成品只能使用一个母版家族。** 开始制作前必须从 `模板/母版/索引.json` 选择唯一 `master_family`；封面、目录、章节页、正文页和结尾页都必须来自这个家族。不得因为某张整页模板好看就把其他母版混入同一成品。
3. **成品中的每一页都必须有两层来源。** provenance 必须同时记录母版家族/外壳来源和内容模块来源。封面、目录、章节或结尾如果由母版家族中的整页角色页承担，可以将其自身记为内容来源；正文页必须记录标题以下模块来自哪一张已验证源页。
4. **正式生产默认只允许三类内容变更：替换文案、替换图片、挂载已提取内容模块。** 母版外壳的页面尺寸、真实母版、获准版式、固定页眉、标题框、主题、字体、颜色和间距必须保持不变。已入库内容模块只能整体复用其原始形状、层级和相对几何，不得重新设计、拆散重排或缩放挤压。研发区内对候选模块的设计不等于正式生产获得了结构修改权。
5. **代码在正式生产中只做机械操作。** 允许用代码抽取原始幻灯片、复制母版家族、复制已验证内容模块、组合页面、替换已有文本或图片、建立索引、渲染和验证；不允许在成品中发明母版、内容模块或版式。仅在用户明确发起的模板研发任务中，代码或 PowerPoint 自动化可以试制一页候选模块，但必须保留原生可编辑形状、不得把整页扁平化为图片，并接受用户审核与后续合同验证。
6. **正式任务没有合适母版角色页或内容模块就停止。** 明确指出缺少封面、目录、章节、结尾或哪一种正文内容模块，不得在任务稿中临时拼一页、绘制一页或用整页图片伪装成 PPT。可以建议转入模板研发流程；只有候选获用户明确批准并完成入库验证后，正式任务才能继续使用它。
7. **内容服从模块容量。** 文案放不下时，先压缩文案、拆分叙事或换一个容量合适的内容模块；不得擅自缩小字号、移动元素、扩大文本框或新增文本框。
8. **图片服从模块中的现有图片框。** 可以替换并在原图片框内裁切，但不得新增图片框、改变图片框几何位置或把生成的整页视觉稿当作一张背景图绕过模板规则。
9. **模块挂载以外的结构变化必须获得明确授权。** 将已登记模块按原坐标复制到选定母版家族的正文安全区，是正式生产中已授权的机械操作；新增、删除、移动、缩放模块内部形状，改变母版外壳或重新排版，仍然超出默认权限。用户在 `模板研发部门/` 发起研发即只授权对当前候选页或明确指定的候选批次进行结构试制，不授权修改母版外壳，也不授权把候选直接用于其他页面。
10. **默认只使用真实、可追溯的图片。** 未经用户明确要求，不得用 AI 生成图、概念图或伪造的界面截图代替真实场景、真实设备和真实事件。优先使用政府机构、科研机构、企业官方媒体库、论文或可信摄影来源。
11. **每张图片都必须有可靠图注和来源。** 图注要准确描述画面中的对象、项目或实验，并写明机构/摄影者及必要的年份；不得凭感觉编造设备名称、地点、日期或版权归属。任务目录必须保存图片来源清单和原始页面 URL。
12. **每个文本框都有容量合同。** 母版角色页和内容模块都必须有容量合同，记录形状名、同名序号、最小字数、最大字数和固定标题行数。文案应接近容量上限但绝不超过，避免文本框明显空洞。
13. **标题行数是母版外壳的一部分。** 母版家族定义为一行的标题必须保持一行，定义为两行的标题必须明确写成两行；不得依赖 PowerPoint 偶然自动换行。每行还要分别满足最大字数限制。
14. **标题行数以视觉设计意图为准。** 不得仅凭源文件当前文案、XML 段落数、显式换行符或一次自动换行结果判断标题是一行还是两行。提取母版家族时必须看渲染效果和标题框的视觉比例；用户确认的行数拥有最高优先级，并要立即回写容量合同，成为后续任务的长期规则。字符数校验通过也不代表视觉宽度一定通过，拉丁字母、数字、空格和粗体 run 可能使标题提前换行；替换后必须以渲染结果复核，若固定行数失败只能压缩文案或改用容量合适的现有模板，不能改字号、标题框或外壳几何。
15. **保留母版家族的关键词异色语法。** 如果原标题用多个文本 run 区分普通文字与强调关键词，替换时必须复用原模板的颜色、字重、字号等 run 样式，把新标题按语义映射为普通词和强调词。不得把混色标题压成单色，也不得自行发明新色板；强调词数量与分布应接近原模板。
16. **对用户只交付最终 PPT。** 构建计划、替换映射、素材原图、来源清单、provenance、渲染预览和过程版本都是内部临时文件，只在制作与验收阶段使用。最终交付目录只能保留一个命名明确的 `.pptx`；不得要求用户在多个版本或工程文件中自行判断。
17. **提取必须从源稿副本删页，禁止插入空白稿。** 母版角色页或单页候选应先完整复制源 PPT 到临时目录，再只删除其他页面并另存。不得把源页插入新建空白演示文稿；该做法会丢失母版元素、主题色和自定义占位符几何。
18. **组合必须以选定母版家族的源稿为母本。** 成品必须从 `母版源.pptx` 的副本开始，在同一文件内部复制该家族的角色页或正文壳页，再挂载内容模块。不得从空白演示文稿开始，也不得用不同整页模板依次充当种子。
19. **允许纯图片模块。** 如果源内容模块确实没有可编辑文字，容量合同使用 `"image_only": true` 和空的 `text_slots`；不得为了通过校验伪造文字槽，也不得跳过该模块。
20. **学习全部页面不等于收录全部页面。** 批量学习时必须审完每一页，但每页都要明确标为 `keep`、`merge` 或 `discard`。只有 `keep` 页可以成为可用母版角色页或内容模块；`merge` 页必须指向一个保留的代表资产，`discard` 页必须写明未完成、不可替换或低复用等原因。
21. **旧整页库只作为候选来源。** `模板/封面`、`模板/目录`、`模板/内容` 中现有整页资产不得跨家族直接拼接。必须先识别其母版家族；正文页只提取标题以下的可复用模块，验证能在目标家族安全区按原坐标容纳后，才能进入 `模板/模块/内容`。
22. **跨母版适配必须只挂载模块形状，绝不复制来源页。** 跨源模块合同必须记录来源 PPT、来源页、来源真实母版指纹、精确形状清单、原始层级和几何签名。挂载时先复制目标家族正文壳，只删除壳内可变正文，再按来源 z-order 复制已登记形状；不得导入来源版式或来源母版。PowerPoint 对连续粘贴的相似形状偶尔会自动施加 10 pt 偏移；机械工具必须检测后把 `Left/Top/Width/Height/Rotation` 恢复为合同源值并再次比较，恢复失败仍须终止，不能把偏移当作允许的重排。完成后必须同时证明目标外壳未漂移、模块几何未漂移、来源母版未残留。
23. **图片替换必须清除旧图的备用渲染链路。** 图片框可能同时保存位图、SVG、链接或异常扩展名内容类型。替换真实图片时，机械工具必须修正媒体 ContentType、移除旧的 `r:link` 与 SVG 备用引用，并在渲染图中确认画面与图注一致；仅检查 `r:embed` 已改变不算完成。
24. **跨母版挂载后必须处理同名形状碰撞。** 如果模块形状名与目标母版固定外壳同名，合同中的 `occurrence` 仍指向来源模块序号，替换映射必须用 `output_occurrence` 单独指定成品中的实际序号；不得改名、删除或误替换母版固定外壳来绕过碰撞。
25. **百页级组合必须分批、暂存提交并支持断点续建。** PowerPoint COM 不得在单次会话中连续复制全部壳页或挂载数百个模块形状；每个短批次先在暂存副本中完成，成功保存后才提交到工作稿，并记录已完成壳页数、原始页清理状态和已挂载模块数。Office 异常后从最后成功批次恢复，不得从头盲目重跑。若某个模块在相同页位连续触发稳定崩溃，应立即从活动索引停用或标记受限，改用另一个已验证且能承担同一沟通任务的模块，不得强行缩放、拆散或重画。
26. **模板研发只能由用户明确发起。** 不得因为模型觉得页面单调就自行设计新模板。研发任务必须说明沟通任务、目标母版家族和希望解决的单调点；默认只研发正文标题以下的内容模块。封面、目录、章节、结尾或母版外壳的研发会改变视觉身份，必须获得用户对该范围的单独明确授权。
27. **默认一次只研发并讨论一页；批量必须由用户明确指定。** 单页模式下，`模板研发部门/create1/` 的当前 `.pptx` 只含一张候选页。用户明确要求研发 N 个模板时，允许把恰好 N 张候选页放进同一份审核 PPTX；每页必须承担不同沟通任务或几何结构，并具有独立候选 ID、审核状态和后续合同。无论单页还是批量，`create1/` 都只保留当前一份 `.pptx`，修改时覆盖它，不堆积 `v1/v2/final2` 等过程版本。
28. **研发候选必须方便拓展、方便延伸。** “可拓展”指沟通任务清晰、替换入口稳定、信息层级可复用，并能自然发展为若干同系列兄弟页或容量变体；不指正式使用时动态新增、移动、缩放形状。两图、三图、四图等衍生结构各自都是独立候选，仍需逐页审核、合同化和验证。
29. **研发候选应尽量图文并茂。** 在内容确有图像价值时，优先设计 1–4 个稳定的真实图片框，并预留与画面对应的短图注/来源位置；同时保留足够、可写满的文字容量和清晰的阅读层级。不得为了“有图”加入无信息价值的装饰图，也不得默认使用 AI 图、伪造截图或不可追溯素材。
30. **用户批准是入库的必要条件，但不是充分条件。** 候选状态只能是 `draft`、`revise`、`approved` 或 `rejected`；只有用户明确表达“通过、可以入库、可以投入使用”等批准意见，才能进入沉淀阶段。批准后还必须冻结源页、建立容量与几何合同、记录母版与素材来源、测试挂载、通过结构/母版/渲染检查并写入活动索引；任何一步失败都不得投入正式使用。
31. **正文模块必须通过节奏去重。** 默认不得在相邻正文页连续使用同一内容模块；任意连续 5 张正文页内，同一模块不得出现超过 2 次。只有内容确实构成同维度并行比较、同组案例或刻意重复的教学节奏时，才允许最多连续 3 页，并在选页计划中写明并行维度和重复理由。不得把“主题相关”误当成并行，也不得让某一个模块成为整套长稿的默认兜底。构建前和验收前都要输出内部复用审计，至少统计总使用次数、最长连续次数和五页窗口密度；超限时先改选现有模块，仍不足则停止并转入模板研发。
32. **用户指定的大纲稿是内容叙事的最高依据。** 当用户提供“大纲 PPT”“内容定稿 PPT”或明确的讲解顺序时，成品扩页必须先建立“成品页 → 大纲源页”的完整映射，保持其章节顺序、核心矛盾、伏笔、回收、小结和结尾落点。130 页可以把 56 页中的一项内容展开成若干证据页，但不得把它们做成互不相干的单页，也不得无理由改换顺序；标为可选的内容可以合并进相邻讲稿或在时间不足时略过，但不能挤掉大纲中的主线页面。
33. **讲稿必须写入原生备注区并服务连续讲述。** 用户要求讲稿时，每页使用 PowerPoint `NotesPage` 的正文占位符，先清除与当前页面无关的历史备注，再写简洁、口语化、可直接讲述的内容。默认结构为“一句本页核心判断 + 一句解释或自然过渡”，不照抄页面文字，不写与页面无关的通用长段，不把讲稿放成幻灯片上的新文本框。交付前必须逐页回读，确认备注数量与页数一致且文本完全匹配。
32. **机械节奏通过后还必须做视觉簇检查。** 相邻重复为零、五页窗口密度合格，只能证明没有明显机械复用，不能证明整稿看起来不重复。百页级成品必须通过全稿缩略图或联系表检查高辨识度模块与同一主视觉的聚集：除明确的并行教学外，同一高辨识度模块或同一张主视觉在任意连续 10 张正文页内默认不得出现超过 2 次。发现“每隔两三页又出现同一结构或同一图片”时，应改选另一种已审核模块或改用同样真实可追溯、但更贴合当前语义的证据图；不得仅靠换颜色、换标题掩盖重复。
32. **“跨母版_四点诊断轴”属于高辨识度框架页，不得作为默认兜底。** 在百页级成品中默认全稿最多使用 2 次，优先承担开场提出核心矛盾、结尾回收判断框架等首尾呼应任务；若确有同维度并行教学需要增加使用，必须获得用户明确同意并在选页计划中记录理由。不能因为它容量宽松、文案容易填充，就把不同叙事任务反复塞进同一四点结构。
32. **研发审核稿必须看起来像已经完成的成品页，而不是线框图或模板说明书。** 候选页必须用真实、具体、可讲述的示例内容把文本槽与图片槽写满；“适用范围、迁移方式、可替换字段、兄弟页方向”等研发元信息只能记录在 `模板研发部门/_internal/`，不得占用审核 PPT 的可见页面。用户审核的是最终视觉潜力，不是空框架。
33. **页面默认遵循“大、满、少而强”。** “大”指主标题、关键数字、正文和结论达到会议投影可读尺度；“满”指核心内容连续占据正文安全区，不能让少量小框漂浮在大片无意义空白中；“少而强”指优先合并碎片化卡片，保持一个视觉主角和少量有明确层级的辅助块。对“通用蓝白”家族的新研发候选，主正文原则上使用 14–18 pt，核心标签 16–20 pt，结论 15–18 pt，图注 9.5–11 pt；低于这一范围必须有源模板或信息密度上的明确理由。不得通过堆叠更多小字来假装页面写满。
34. **字号和排版必须先对标已有高质量模板，再按当前几何校准。** 不得只凭模型经验设一套全局字号。研发前至少选择一张用户亲自校准页和两张已审核/活动模板，实测其“标题带—板块标题—宽正文—窄卡正文—结论—图注”层级与正文占用；新候选应复用这种相对层级。对“通用蓝白”家族，当前经验标尺通常为：标题带 24–26 pt，板块标题 18–28 pt，宽正文 16–20 pt，窄卡正文可按已审核源降至约 11.5–15 pt，结论 18–20 pt，图注 9.5–11 pt。数值只是经验证模板的参考区间，最终必须看文本框宽度、行数和渲染效果；宁可压缩文案或把横向窄列改成纵向层级，也不能把整页层级压平到接近字号。
35. **讲座型成品的开场角色必须收敛，不能在目录前连续堆论证页。** 当成品包含讲者/团队介绍时，默认开场顺序为“封面 → 团队 → 一页全场总览 → 唯一目录 → 第一部分正文”；总览只负责建立今天要回答什么、按哪几段展开和贯穿全场的判断主线，目录只负责给出稳定章节名称。核心矛盾、方法论证、工程链、数据闭环或案例证据必须移动到对应章节内部，不能突然插在团队与总览/目录之间。只有用户指定的大纲源稿明确要求另一种开场顺序时才能偏离，并要在页序映射中记录理由。

## 目录职责

- `待提取/`：用户提供、尚未整理的历史 PPT 或参考 PPT。
- `模板/母版/<家族>/母版源.pptx`：一个母版家族的保真源稿，是组合成品的唯一种子。
- `模板/母版/<家族>/family.json`：记录真实母版、获准版式、角色页、固定外壳、正文安全区和品牌样式。
- `模板/母版/索引.json`：母版家族索引；未指定时使用其中登记的默认家族。
- `模板/模块/内容/`：经验证的标题以下内容模块及其容量、几何、来源和兼容性合同。
- `模板/封面/`、`模板/目录/`、`模板/内容/`：历史整页模板库，只作为母版家族和内容模块的候选来源，不再直接跨家族组合成品。
- `模板研发部门/create1/`：用户与模型共同审核新模板的交互区；只保留一份当前候选 `.pptx`。默认一页；用户明确要求批量时可含指定数量的独立候选页。不放代码、素材、预览、合同或过程版本。
- `模板研发部门/_internal/`：研发内部工作区，保存需求简报、真实图片及来源、替换映射、渲染预览、评审状态和验证证据。
- `模板研发部门/已审核/`：用户已明确批准的候选源页归档；这里的文件仍须完成模块合同、兼容性测试和活动索引登记后才能用于正式任务。
- `已提取/`：已经完成筛选和提取的原始 PPT；移动到这里前要确认模板和索引均已生成。
- `task/<任务名>/`：内部临时工作区，可存放需求、文案、图片、选页计划、provenance 和预览；完成交付后清理。
- `test/`：当前测试交付目录，只保留一个最终 `.pptx`，不放代码、映射、图片、预览、侧车文件或过程版本。
- `script/`：本项目所有 PPT 机械化工具的统一目录。

内容模块文件名要表达沟通任务和结构，而不是只写“第 3 页”，例如 `流程拆解_五节点_双总结`。模块合同必须记录源页、源母版家族、边界框和全部形状原始坐标。

## 标准工作流

1. 先理解任务，形成逐页叙事与素材需求。
2. 先选且只选一个 `master_family`。封面、目录、章节、正文壳和结尾必须都能由该家族提供；缺任何关键角色就停止并补充家族资产。
3. 再为每一张正文页选择标题以下的内容模块，读取模块容量合同，并验证模块边界框能原尺寸落入该家族正文安全区。缺少模块时停止正式构建；如用户希望补充新结构，另行进入模板研发流程，待批准并入库后再返回本步骤。
4. 写分层选页计划：每页同时记录 `master_family`、角色页/正文壳来源和 `content_module` 来源；确保页面尺寸一致。随后执行模块复用审计，修正无叙事理由的相邻重复和五页窗口超密度；获准的并行重复必须记录共同维度、页码范围和理由。百页稿还要预先统计高辨识度模块和主视觉的十页窗口聚集度，避免机械规则合格但视觉上仍反复出现同一页。
5. 复制该家族 `母版源.pptx` 到内部工作区；只在这一母本内复制家族角色页和正文壳页，不从空白稿或其他母版插入整页。
6. 使用 `python script/template_ppt.py compose-family` 从目标家族母版源副本构建：同源模块在母本内部按稳定 SlideID 复制；跨源模块只把合同登记的标题线以下形状按原坐标与原层级挂到目标正文壳，绝不插入来源整页或导入来源母版。然后只替换已有文案和图片。
7. 运行结构验证与母版一致性验证：所有页面必须指向同一个真实 PowerPoint 母版，使用获准版式；固定外壳不漂移，模块形状和几何与来源合同一致。
8. 渲染全部页面并逐页目视检查：统一页眉/标题系统、文本框是否写满但未溢出、标题行数、遮挡、图片裁切、图注与图片是否对应、错字、低清图片、风格跳变和叙事连续性。长稿必须额外生成全稿联系表，从远观节奏复核高辨识度模块与同图是否形成视觉簇；联系表发现的问题要回到选页计划修正后重新组合和验证。
9. 只有结构验证、母版一致性验证和视觉检查都通过，才把唯一最终 `.pptx` 放入交付目录；随后清理交付目录中的过程版本和全部非 PPT 文件。

## 模板研发部门工作流

1. 用户明确提出“创建新模板”后，先把需求压缩成单页沟通任务；批量研发则把指定数量拆成同样数量、彼此不重复的单页沟通任务。每页都要说明表达目标、现有模块为何不适用、主要信息块、图片数量、预计文字容量，以及可能延伸的兄弟页。
2. 从 `模板/母版/索引.json` 选择目标 `master_family`，复制其 `母版源.pptx` 中已批准的正文壳页作为唯一画布。保持真实母版、版式、固定页眉、标题框、主题、字体和颜色系统不变；默认只研发标题线以下的内容模块。
3. 默认只制作一张原生可编辑候选页；用户明确要求批量时，制作恰好指定数量的候选页，并确保每页结构实质不同。围绕各自沟通任务组织文字层级和图片框，优先形成可复用的稳定结构；不得用整页图片、SVG 页面或截图伪装成 PPT，也不得额外提交未请求的备选套数让用户自行筛选。
4. 按“方便拓展、方便延伸”设计：明确哪些内容可以替换、容量上下限、图片数量、自然的叙事延伸方向及可能的兄弟模块，但不在当前候选中预埋会破坏对齐和容量合同的任意增删机制。
5. 按“尽量图文并茂”设计：有真实视觉证据时优先使用图片框，并为每张图提供可信图注和来源位置；没有合适真实图片时宁可保留清晰的图片替换占位说明，也不得编造图像和图注。
6. 把候选页当成成品示例页来完成：使用真实示例文案与图片写满页面，删除可见的“适用、迁移、替换说明”等研发元信息；按“大、满、少而强”检查正文安全区占用、核心字号、视觉主角与碎片数量。
7. 自检并渲染全部候选页，检查母版外壳无漂移、标题行数和异色语法正确、文字写满但不溢出、图片裁切合理、图注对应、无重叠和越界。还要检查主正文字号、无意义空白和碎片化小框；仅结构正确但视觉稀疏或小字过多仍判定失败。之后将 `create1/` 清理为只剩一份当前审核 `.pptx`；单页模式为一页，批量模式必须与用户指定数量一致。
8. 用户提出修改时把相应候选状态记为 `revise`，只修改同一候选页并覆盖当前交互文件；每轮都重新渲染检查。批量审核必须逐页保存状态，用户未明确批准的页面始终处于隔离状态，不得进入任务稿或活动索引。
9. 用户明确批准后，把状态记为 `approved`：冻结一份不可变源页到 `模板研发部门/已审核/`，在 `模板/模块/内容/` 建立模块合同，记录来源 PPT、页码、目标母版指纹、精确形状清单、z-order、原始几何、容量、固定标题行数、run 样式、图片框和兼容家族。
10. 对批准候选执行测试挂载、`verify-module`、`verify-layered`、`verify-master` 和全页渲染目检。全部通过后才写入 `模板/模块/索引.json` 并标记 active；这一步完成后，正式任务才可以像复用其他模板一样只替换文案和图片。
11. 用户拒绝的候选记为 `rejected`，不得入库或投入使用；只在内部审核记录中保留结论，`create1/` 继续作为下一次单页研发的干净交互区。

## 模板积累原则

- 从 `待提取/` 中挑选真正可复用、视觉完成度高的页面，不盲目把整份 PPT 全部入库。
- 先学习母版家族，再学习内容模块。母版家族负责统一的封面、目录、章节、正文标题外壳和结尾；正文页只有标题以下部分进入模块学习。
- 固定外壳与内容模块的边界必须通过整稿渲染逐页判断，并写入 `family.json`。不能只按 XML 层级机械判断，因为固定标题对象可能位于幻灯片层。
- 同一个源稿的正文模块可以保留其原始绝对坐标；跨家族复用前必须证明其边界框完整落入目标正文安全区。不得为了适配而缩放或重排模块。
- “全审”与“全收”必须分开：学习记录覆盖源稿每一页，模板目录只保留通过策展的代表页。
- 每页策展只能有一个结论：`keep` 表示独立结构且完成度高；`merge` 表示同构、旧版或同一沟通任务的内容变体，并必须指向质量更高、适用更广的 `keep` 页；`discard` 表示未完成、视觉层级弱、主要内容固化在不可拆分复合图片中，或没有稳定替换价值。
- 先按“沟通任务 + 几何结构 + 可替换入口”建立相似组，再比较版本新旧、完成度、信息容量与通用性。相同章节导航的高亮状态、同布局的项目经费/配套经费、同一截图页的内容变体都不得重复占库。
- 抽取后按 `封面 / 目录 / 内容` 分类，并为页面写清楚用途标签。抽取必须采用“复制完整源稿后删除其他页”的保真方法。
- 保留原始母版、主题、字体和全部视觉对象；提取是复制，不是重画。
- 为每个模板建立 `.pptx.constraints.json`，实测并记录所有必填文本框的容量、固定行数，以及混色标题的原始 run 样式角色。标题行数必须来自渲染后的视觉判断或用户确认，不能从 XML 机械推断；没有容量合同的页面不能进入可用索引。
- 同构页面只保留质量最高、适用范围最广的版本，避免模板库堆积重复页面。
- 模板库必须覆盖不同沟通任务和阅读节奏，不能只积累同一种卡片结构的配色变体。研发优先补足主视觉证据、时间演进、机制剖面、前后对照、量化结果和决策选择等真正改变信息组织方式的模块。
- 每次完成新任务后，只有经过验证且具有复用价值的页面才回流模板库。
- 模板研发候选与历史源稿候选使用同一质量门槛：用户审核决定是否值得沉淀，合同和验证决定是否能够安全复用。`approved` 源页不能绕过 `keep`、容量合同、兼容性测试和活动索引直接进入成品。
- 对整份源稿执行学习任务时，每一页都必须进入 `模板/学习记录.json`。`learn` 产生的是待策展候选，不是最终入库结果；必须完成 `模板/策展记录.json`，运行 `python script/curate_template_library.py --apply`，并确认 `curation_status` 为 `passed` 后才能刷新索引或归档源稿。
- 策展通过后，只对 `keep` 页建立可用索引与容量合同；`merge` 和 `discard` 页仍保留在学习记录与策展结果中，确保空白智能体知道它们已审过而不是漏学。

## 完成定义

一份 PPT 只有同时满足以下条件才算完成：

- 全套只使用一个已登记母版家族，母版一致性验证通过；
- 每页均有母版外壳来源记录，正文页另有内容模块来源记录；
- 没有代码生成的自创版式；
- 只发生了获准的文案和图片替换；
- 所有图片均为真实、可追溯素材，图注与来源清单一致；
- 所有必填文本框满足最小/最大字数，所有固定标题保持模板规定的行数和关键词异色语法；
- 固定外壳与内容模块的结构、层级和几何验证通过；
- 全页渲染并完成目视检查；
- 构建阶段已用 provenance、来源记录和预览完成核验；
- 面向用户的交付目录中只有一个最终 `.pptx`。
